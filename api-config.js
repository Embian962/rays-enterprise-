// The storefront calls the Render API directly so admin Authorization headers
// are preserved when products, orders, and other protected data are changed.
window.RAYS_API_URL = "https://rays-enterprise-l7qp.onrender.com";
